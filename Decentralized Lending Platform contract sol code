function createLoanRequest(uint256 amount, uint256 duration) public {
    loanRequests[nextLoanId] = LoanRequest({
        borrower: msg.sender,
        amount: amount,
        duration: duration,
        isApproved: false
    });

    borrowerLoans[msg.sender].push(nextLoanId);
    nextLoanId++;
}
